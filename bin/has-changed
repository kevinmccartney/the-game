#!/usr/bin/env python

import sys
import json

print(sys.argv[0])
print(sys.argv[1])

changed_arg = sys.argv[1]

print(type(changed_arg))

changed = json.loads(changed_arg)

if len(changed["changes"]) > 0 and "__no-changes" not in changed["changes"]:
    print("yes")
else:
    print("no")
