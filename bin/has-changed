#!/usr/bin/env python

import json

with open("the-game-changed-api-project.json", "r") as file:
    contents = file.read()
    changed = json.loads(contents)

    if len(changed["changes"]) > 0 and "__no-changes" not in changed["changes"]:
        print("yes")
    else:
        print("no")
