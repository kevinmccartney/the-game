#!/usr/bin/env python

import sys
import json

changed_arg = sys.argv[1]

changed = json.loads(changed_arg)

if len(changed) > 0 and "__no-changes" not in changed:
    print("yes")
else:
    print("no")
